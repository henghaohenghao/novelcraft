# NovelCraft Backend - Requirements for V2.0

# Core Framework
fastapi==0.121.3
uvicorn[standard]==0.38.0
pydantic==2.12.5
pydantic-settings==2.11.0

# Database
sqlalchemy[asyncio]==2.0.44
asyncpg==0.29.0
psycopg2-binary==2.9.12
aiosqlite==0.20.0

# Graph Database
neo4j==6.1.0

# Vector Database
qdrant-client==1.18.0

# Redis
redis[hiredis]==6.4.0

# AI & LLM
langchain==1.2.12
langchain-openai==1.1.11
langgraph==1.1.2
langgraph-prebuilt==1.0.8
langgraph-checkpoint==4.0.1
langgraph-sdk==0.3.11
openai==2.28.0
tiktoken==0.11.0
transformers==4.56.2

# Embedding（云服务器无 GPU，用 CPU 版 torch，约 200MB vs GPU 版 2.5GB）
# torch 不在此指定版本，由 Dockerfile.backend 通过 --extra-index-url 安装 CPU 版
sentence-transformers==5.3.0

# Temporal Workflow (V2.0 新增) - 可选依赖
# temporalio==1.7.1  # 如需使用工作流功能，请取消注释并安装

# WebSocket & Real-time (V2.0 新增)
python-socketio==5.16.0
websockets==15.0.1

# Celery
celery[redis]==5.6.3

# Utilities
python-multipart==0.0.12
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
httpx==0.28.1
numpy==1.26.4
beautifulsoup4==4.14.3

# Development（生产镜像不安装）
# pytest==8.3.3
# pytest-asyncio==0.24.0
# black==24.10.0
# ruff==0.7.3
